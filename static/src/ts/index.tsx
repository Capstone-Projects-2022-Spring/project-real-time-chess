import Homepage from './views/Homepage';
import * as ReactDOM from 'react-dom';
import * as React from 'react';

ReactDOM.render(
    <div>
        <Homepage />
    </div>,
    document.getElementById('react-app-target')
);
